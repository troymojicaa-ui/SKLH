export default function Hero() {
  return (
    <section className="relative isolate overflow-hidden bg-gray-200">
      {/* Big hero area (200% taller than before) */}
      <div className="mx-auto max-w-7xl px-6 py-64 md:py-[32rem]">
        {/* Placeholder for hero image */}
        <div className="h-full w-full rounded-xl bg-gray-300 flex items-center justify-center">
          <span className="text-gray-500 text-lg">Hero Image Placeholder</span>
        </div>
      </div>
    </section>
  );
}
